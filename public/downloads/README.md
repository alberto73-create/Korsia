# APK downloads

Metti qui l'APK generato e rinominalo `speed-guard.apk` per abilitarne il download diretto dal sito:

```bash
cp android/app/build/outputs/apk/debug/app-debug.apk public/downloads/speed-guard.apk
```

Non committare APK reali pesanti senza una decisione di release.
